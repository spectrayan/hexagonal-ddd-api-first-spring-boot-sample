/**
 * Application use case implementations.
 *
 * <p>Use cases coordinate domain operations by invoking input ports (from inbound
 * adapters) and output ports (toward external systems). They remain thin and
 * free from transport and persistence concerns.</p>
 */
package com.spectrayan.intake.application.usecase;
